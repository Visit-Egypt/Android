<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.visitegypt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisitEgypt">

        <activity
            android:name=".presentation.signup.SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.detailplace.DetailActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".presentation.home.HomeActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".presentation.signin.SignInActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".presentation.chatbot.ChatbotModel"
            android:exported="true"
            tools:ignore="Instantiatable">

        </activity>
    </application>

</manifest>